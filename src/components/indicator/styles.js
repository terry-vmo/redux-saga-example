import { ScaledSheet } from 'react-native-size-matters';

export const IndicatorStyles = ScaledSheet.create({
  container: {
    right: 0,
    left: 0,
    bottom: 0,
    top: 0,
    position: 'absolute',
  },
});

export default IndicatorStyles;
